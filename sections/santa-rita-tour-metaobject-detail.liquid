{% liquid
  assign current_tour = metaobject
  assign tour_title = current_tour.title.value | default: current_tour.title | default: current_tour.system.title
  assign hero_image = current_tour.banner_image.value | default: current_tour.banner_image
  assign hero_subheading = current_tour.hero_subheading.value | default: current_tour.hero_subheading
  assign short_description = current_tour.short_description.value | default: current_tour.short_description
  assign duration = current_tour.duration.value | default: current_tour.duration
  assign people = current_tour.people.value | default: current_tour.people
  assign languages = current_tour.languages.value | default: current_tour.languages
  assign wines = current_tour.wines.value | default: current_tour.wines
  assign route_heading = current_tour.route_heading.value | default: current_tour.route_heading
  assign route_stops_text = current_tour.route_stops.value | default: current_tour.route_stops
  assign route_stop_lines = route_stops_text | newline_to_br | split: '<br />'
  assign experience_heading = current_tour.experience_heading.value | default: current_tour.experience_heading
  assign experience_text = current_tour.experience_text
  assign includes_heading = current_tour.includes_heading.value | default: current_tour.includes_heading
  assign includes_text = current_tour.includes.value | default: current_tour.includes
  assign include_lines = includes_text | newline_to_br | split: '<br />'
  assign time_options_source = current_tour.time_options.value | default: current_tour.time_options | default: section.settings.time_options
  assign language_options_source = current_tour.language_options.value | default: current_tour.language_options | default: section.settings.language_options
  assign max_people = current_tour.max_people.value | default: current_tour.max_people | default: section.settings.max_people
  assign disabled_weekdays = current_tour.disabled_weekdays.value | default: current_tour.disabled_weekdays
  assign blocked_dates = current_tour.blocked_dates.value | default: current_tour.blocked_dates
  assign enabled_dates = current_tour.enabled_dates.value | default: current_tour.enabled_dates
  assign booking_url = current_tour.booking_url.value | default: current_tour.booking_url | default: section.settings.booking_url
  assign tour_price = current_tour.price.value | default: current_tour.price
  assign price_amount = tour_price.amount | default: tour_price | default: 0
  assign price_cents = price_amount | times: 100 | round
  assign price_currency = tour_price.currency_code | default: cart.currency.iso_code | default: shop.currency
  assign booking_url_string = booking_url | append: ''
  assign price_amount_string = price_amount | append: ''
  assign time_options = time_options_source | split: ','
  assign language_options = language_options_source | split: ','
%}

{{ 'https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css' | stylesheet_tag }}
<script src="https://cdn.jsdelivr.net/npm/flatpickr" defer></script>
<script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/es.js" defer></script>

<santa-rita-tour-metaobject-detail
  class="santa-rita-tour-detail"
  data-section-id="{{ section.id }}"
  data-tour-title="{{ tour_title | escape }}"
  data-tour-handle="{{ current_tour.system.handle | escape }}"
  data-price-amount="{{ price_amount_string | escape }}"
  data-price-currency="{{ price_currency | escape }}"
  data-booking-url="{{ booking_url_string | escape }}"
  data-disabled-weekdays="{{ disabled_weekdays | escape }}"
  data-blocked-dates="{{ blocked_dates | escape }}"
  data-enabled-dates="{{ enabled_dates | escape }}"
  {{ section.shopify_attributes }}
>
  <section class="santa-rita-tour-detail__hero" aria-labelledby="santa-rita-tour-title-{{ section.id }}">
    {% if hero_image != blank %}
      {{
        hero_image
        | image_url: width: 2200
        | image_tag:
          widths: '480, 720, 990, 1440, 1800, 2200',
          sizes: '100vw',
          loading: 'eager',
          class: 'santa-rita-tour-detail__hero-image'
      }}
    {% elsif request.design_mode %}
      {{ 'image' | placeholder_svg_tag: 'santa-rita-tour-detail__hero-image santa-rita-tour-detail__hero-image--placeholder' }}
    {% endif %}

    <div class="santa-rita-tour-detail__hero-overlay"></div>

    <div class="santa-rita-tour-detail__hero-inner">
      <nav class="santa-rita-tour-detail__breadcrumbs" aria-label="Breadcrumb">
        <a href="{{ routes.root_url }}">Inicio</a>
        <span aria-hidden="true">&gt;</span>
        {% if section.settings.tours_page != blank %}
          <a href="{{ section.settings.tours_page.url }}">Tours</a>
        {% else %}
          <span>Tours</span>
        {% endif %}
        <span aria-hidden="true">&gt;</span>
        <span>{{ tour_title }}</span>
      </nav>

      <div class="santa-rita-tour-detail__hero-content">
        <h1 id="santa-rita-tour-title-{{ section.id }}">{{ tour_title }}</h1>

        {% if hero_subheading != blank %}
          <p>{{ hero_subheading }}</p>
        {% elsif short_description != blank %}
          <p>{{ short_description | strip_html | truncatewords: 22 }}</p>
        {% endif %}

        <strong>{{ price_cents | money }}</strong>
        {% if section.settings.price_note != blank %}
          <span>{{ section.settings.price_note }}</span>
        {% endif %}
      </div>
    </div>
  </section>

  <div class="santa-rita-tour-detail__content">
    <div class="santa-rita-tour-detail__layout">
      <div class="santa-rita-tour-detail__main">
        <div class="santa-rita-tour-detail__metrics">
          {% if duration != blank %}
            <div class="santa-rita-tour-detail__metric">
              <span>
                <svg viewBox="0 0 24 24" fill="none" focusable="false" aria-hidden="true">
                  <path d="M12 7v5l3 2M12 21a8 8 0 1 0 0-16 8 8 0 0 0 0 16ZM9 2h6" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
              <strong>{{ duration }}</strong>
              <small>duracion</small>
            </div>
          {% endif %}

          {% if people != blank %}
            <div class="santa-rita-tour-detail__metric">
              <span>
                <svg viewBox="0 0 24 24" fill="none" focusable="false" aria-hidden="true">
                  <path d="M12 12a4 4 0 1 0 0-8 4 4 0 0 0 0 8ZM4.5 21a7.5 7.5 0 0 1 15 0" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
              <strong>{{ people }}</strong>
              <small>personas</small>
            </div>
          {% endif %}

          {% if languages != blank %}
            <div class="santa-rita-tour-detail__metric">
              <span>
                <svg viewBox="0 0 24 24" fill="none" focusable="false" aria-hidden="true">
                  <path d="M3 12h18M12 3a15 15 0 0 1 0 18M12 3a15 15 0 0 0 0 18M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
              <strong>{{ languages }}</strong>
              <small>idiomas</small>
            </div>
          {% endif %}

          {% if wines != blank %}
            <div class="santa-rita-tour-detail__metric">
              <span>
                <svg viewBox="0 0 24 24" fill="none" focusable="false" aria-hidden="true">
                  <path d="M8 3.5h8v6.2c0 2.1-1.35 3.85-3.2 4.42V18h3.7M7.5 18h9M8 7h8M6 5.5h2M16 5.5h2" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
              <strong>{{ wines }}</strong>
              <small>vinos</small>
            </div>
          {% endif %}
        </div>

        {% if route_stops_text != blank %}
          <section class="santa-rita-tour-detail__route">
            {% if route_heading != blank %}
              <h2>{{ route_heading }}</h2>
            {% endif %}

            <div class="santa-rita-tour-detail__route-items">
              {% for route_stop in route_stop_lines %}
                {% assign route_stop = route_stop | strip %}
                {% if route_stop != blank %}
                  <article class="santa-rita-tour-detail__route-item">
                    <span>{{ forloop.index | prepend: '0' | slice: -2, 2 }}</span>
                    <h3>{{ route_stop }}</h3>
                  </article>
                {% endif %}
              {% endfor %}
            </div>
          </section>
        {% endif %}

        {% if experience_heading != blank or experience_text != blank or short_description != blank %}
          <section class="santa-rita-tour-detail__experience">
            {% if experience_heading != blank %}
              <h2>{{ experience_heading }}</h2>
            {% endif %}

            {% if experience_text != blank %}
              <div>{{ experience_text | metafield_tag }}</div>
            {% elsif short_description != blank %}
              <div><p>{{ short_description }}</p></div>
            {% endif %}
          </section>
        {% endif %}

        {% if includes_text != blank %}
          <section class="santa-rita-tour-detail__includes">
            {% if includes_heading != blank %}
              <h2>{{ includes_heading }}</h2>
            {% endif %}

            <div class="santa-rita-tour-detail__includes-grid">
              {% for include_line in include_lines %}
                {% assign include_line = include_line | strip %}
                {% if include_line != blank %}
                  {% assign include_parts = include_line | split: '|' %}
                  {% assign include_icon = include_parts[0] | strip %}
                  {% assign include_text = include_parts[1] | default: include_parts[0] | strip %}
                  <div class="santa-rita-tour-detail__include">
                    {% if include_parts[1] != blank %}
                      <span aria-hidden="true">{{ include_icon }}</span>
                    {% endif %}
                    <p>{{ include_text }}</p>
                  </div>
                {% endif %}
              {% endfor %}
            </div>
          </section>
        {% endif %}
      </div>

      <aside class="santa-rita-tour-detail__reserve" aria-labelledby="santa-rita-tour-reserve-title-{{ section.id }}">
        <h2 id="santa-rita-tour-reserve-title-{{ section.id }}">
          {{ section.settings.reserve_heading }}
          <span aria-hidden="true">
            <svg viewBox="0 0 24 24" fill="none" focusable="false">
              <path d="M8 3.5h8v6.2c0 2.1-1.35 3.85-3.2 4.42V18h3.7M7.5 18h9M8 7h8M6 5.5h2M16 5.5h2" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
          </span>
        </h2>

        <div class="santa-rita-tour-detail__reserve-body">
          {% if section.settings.reserve_info_heading != blank %}
            <h3 class="santa-rita-tour-detail__reserve-info-heading">{{ section.settings.reserve_info_heading }}</h3>
          {% endif %}

          <form class="santa-rita-tour-detail__form" data-tour-booking-form>
            <div class="santa-rita-tour-detail__field">
              <label for="TourLanguage-{{ section.id }}">{{ section.settings.language_label }}</label>
              <select id="TourLanguage-{{ section.id }}" name="idioma" required data-tour-language>
                {% for option in language_options %}
                  {% assign option_value = option | strip %}
                  {% if option_value != blank %}
                    <option value="{{ option_value | escape }}">{{ option_value }}</option>
                  {% endif %}
                {% endfor %}
              </select>
            </div>

            <div class="santa-rita-tour-detail__fields">
              <div class="santa-rita-tour-detail__field">
                <label for="TourAdults-{{ section.id }}">{{ section.settings.adults_label }}</label>
                <select id="TourAdults-{{ section.id }}" name="adultos" required data-tour-adults>
                  {% for adult_count in (1..max_people) %}
                    <option value="{{ adult_count }}">{{ adult_count }}</option>
                  {% endfor %}
                </select>
              </div>

              <div class="santa-rita-tour-detail__field">
                <label for="TourChildren-{{ section.id }}">{{ section.settings.children_label }}</label>
                <select id="TourChildren-{{ section.id }}" name="ninos" data-tour-children>
                  {% for child_count in (0..max_people) %}
                    <option value="{{ child_count }}">{{ child_count }}</option>
                  {% endfor %}
                </select>
              </div>
            </div>

            <div class="santa-rita-tour-detail__field santa-rita-tour-detail__field--calendar">
              <label for="TourDate-{{ section.id }}">{{ section.settings.date_label }}</label>
              <input
                id="TourDate-{{ section.id }}"
                type="text"
                name="fecha"
                placeholder="{{ section.settings.date_placeholder }}"
                required
                data-tour-calendar
              >
            </div>

            <div class="santa-rita-tour-detail__field">
              <label for="TourTime-{{ section.id }}">{{ section.settings.time_label }}</label>
              <select id="TourTime-{{ section.id }}" name="horario" required data-tour-time>
                {% for option in time_options %}
                  {% assign option_value = option | strip %}
                  {% if option_value != blank %}
                    <option value="{{ option_value | escape }}">{{ option_value }}</option>
                  {% endif %}
                {% endfor %}
              </select>
            </div>

            <div class="santa-rita-tour-detail__total">
              <span>{{ section.settings.total_label }}</span>
              <strong data-tour-total>{{ price_cents | money }}</strong>
            </div>

            <button
              type="submit"
              class="santa-rita-tour-detail__submit"
              {% if booking_url == blank %}
                disabled
              {% endif %}
            >
              {{ section.settings.reserve_button_label }}
            </button>
          </form>
        </div>
      </aside>
    </div>
  </div>
</santa-rita-tour-metaobject-detail>

<script>
  if (!customElements.get('santa-rita-tour-metaobject-detail')) {
    customElements.define(
      'santa-rita-tour-metaobject-detail',
      class SantaRitaTourMetaobjectDetail extends HTMLElement {
        connectedCallback() {
          this.form = this.querySelector('[data-tour-booking-form]');
          this.calendarInput = this.querySelector('[data-tour-calendar]');
          this.languageInput = this.querySelector('[data-tour-language]');
          this.timeInput = this.querySelector('[data-tour-time]');
          this.adultsInput = this.querySelector('[data-tour-adults]');
          this.childrenInput = this.querySelector('[data-tour-children]');
          this.totalElement = this.querySelector('[data-tour-total]');
          this.priceAmount = Number(this.dataset.priceAmount || 0);
          this.currency = this.dataset.priceCurrency || 'CLP';

          [this.adultsInput, this.childrenInput].forEach((input) => {
            input?.addEventListener('change', () => this.updateTotal());
          });

          this.form?.addEventListener('submit', (event) => this.handleSubmit(event));
          this.updateTotal();
          this.initCalendarWhenReady();
        }

        getPeopleCount() {
          const adults = Math.max(1, Number(this.adultsInput?.value || 1));
          const children = Math.max(0, Number(this.childrenInput?.value || 0));

          return adults + children;
        }

        updateTotal() {
          if (!this.totalElement) return;

          const total = this.priceAmount * this.getPeopleCount();

          this.totalElement.textContent = new Intl.NumberFormat('es-CL', {
            style: 'currency',
            currency: this.currency,
            maximumFractionDigits: 0
          }).format(total);
        }

        handleSubmit(event) {
          event.preventDefault();

          if (!this.form?.reportValidity()) return;

          const bookingUrl = this.dataset.bookingUrl;
          if (!bookingUrl) return;

          const url = new URL(bookingUrl, window.location.origin);
          const people = this.getPeopleCount();
          const total = this.priceAmount * people;

          url.searchParams.set('tour', this.dataset.tourHandle || '');
          url.searchParams.set('tour_title', this.dataset.tourTitle || '');
          url.searchParams.set('idioma', this.languageInput?.value || '');
          url.searchParams.set('adultos', this.adultsInput?.value || '1');
          url.searchParams.set('ninos', this.childrenInput?.value || '0');
          url.searchParams.set('personas', String(people));
          url.searchParams.set('fecha', this.calendarInput?.value || '');
          url.searchParams.set('horario', this.timeInput?.value || '');
          url.searchParams.set('precio', String(this.priceAmount));
          url.searchParams.set('total', String(total));
          url.searchParams.set('currency', this.currency);

          window.location.href = url.toString();
        }

        initCalendarWhenReady() {
          let attempts = 0;

          const run = () => {
            attempts += 1;

            if (window.flatpickr && this.calendarInput) {
              this.initCalendar();
              return;
            }

            if (attempts < 80) {
              window.setTimeout(run, 100);
            }
          };

          run();
        }

        initCalendar() {
          if (this.calendarInput.dataset.flatpickrReady === 'true') return;

          const disabledWeekdays = this.parseList(this.dataset.disabledWeekdays).map((day) => Number(day));
          const blockedDates = this.parseList(this.dataset.blockedDates);
          const enabledDates = this.parseList(this.dataset.enabledDates);
          const disable = blockedDates.slice();
          const today = new Date();
          const todayKey = this.formatDateKey(today);
          const isTodayBlocked = blockedDates.includes(todayKey) || disabledWeekdays.includes(today.getDay());
          const isTodayEnabled = enabledDates.length === 0 || enabledDates.includes(todayKey);

          if (disabledWeekdays.length > 0) {
            disable.push((date) => disabledWeekdays.includes(date.getDay()));
          }

          const options = {
            minDate: 'today',
            dateFormat: 'Y-m-d',
            altInput: true,
            altFormat: 'd-m-Y',
            inline: true,
            locale: window.flatpickr.l10ns?.es || {
              firstDayOfWeek: 1
            },
            disable
          };

          if (!isTodayBlocked && isTodayEnabled) {
            options.defaultDate = 'today';
          }

          if (enabledDates.length > 0) {
            options.enable = enabledDates;
          }

          window.flatpickr(this.calendarInput, options);
          this.calendarInput.dataset.flatpickrReady = 'true';
        }

        parseList(value) {
          return String(value || '')
            .split(',')
            .map((item) => item.trim())
            .filter(Boolean);
        }

        formatDateKey(date) {
          const year = date.getFullYear();
          const month = String(date.getMonth() + 1).padStart(2, '0');
          const day = String(date.getDate()).padStart(2, '0');

          return `${year}-${month}-${day}`;
        }
      }
    );
  }
</script>

<style>
  .santa-rita-tour-detail {
    display: block;
    background: #fffbf6;
    color: #21000f;
  }

  .santa-rita-tour-detail__hero {
    position: relative;
    display: grid;
    min-height: clamp(310px, 31vw, 430px);
    overflow: hidden;
    background: #21000f;
    color: #ffffff;
  }

  .santa-rita-tour-detail__hero-image,
  .santa-rita-tour-detail__hero-image--placeholder {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .santa-rita-tour-detail__hero-image--placeholder {
    background: #2c0518;
    fill: rgba(255, 255, 255, 0.2);
  }

  .santa-rita-tour-detail__hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(180deg, rgba(33, 0, 15, 0.42), rgba(33, 0, 15, 0.72));
  }

  .santa-rita-tour-detail__hero-inner {
    position: relative;
    z-index: 1;
    display: flex;
    width: min(100% - 48px, 1140px);
    min-height: inherit;
    flex-direction: column;
    margin-inline: auto;
    padding-block: 24px 56px;
  }

  .santa-rita-tour-detail__breadcrumbs {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
    align-items: center;
    font-size: 12px;
    line-height: 1.2;
  }

  .santa-rita-tour-detail__breadcrumbs a {
    color: inherit;
    text-decoration: none;
  }

  .santa-rita-tour-detail__hero-content {
    width: min(100%, 720px);
    margin: auto auto 0;
    text-align: center;
  }

  .santa-rita-tour-detail__hero-content h1 {
    margin: 0 0 12px;
    font-family: var(--font-heading--family);
    font-size: clamp(36px, 4.1vw, 58px);
    font-weight: 700;
    line-height: 1.02;
    letter-spacing: 0;
  }

  .santa-rita-tour-detail__hero-content p {
    width: min(100%, 560px);
    margin: 0 auto 14px;
    font-size: 16px;
    line-height: 1.42;
  }

  .santa-rita-tour-detail__hero-content strong,
  .santa-rita-tour-detail__hero-content span {
    display: inline-block;
    font-size: 16px;
  }

  .santa-rita-tour-detail__content {
    width: min(100% - 48px, 1140px);
    margin-inline: auto;
    padding-block: 34px 74px;
  }

  .santa-rita-tour-detail__metrics {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 0;
    overflow: hidden;
    margin-bottom: 0;
    border-radius: 6px;
    background: #fff8f1;
    box-shadow: 0 2px 8px rgba(140, 26, 62, 0.04);
  }

  .santa-rita-tour-detail__metric {
    display: grid;
    justify-items: center;
    gap: 7px;
    min-height: 126px;
    background: transparent;
    padding: 24px 12px 20px;
    text-align: center;
  }

  .santa-rita-tour-detail__metric span {
    display: grid;
    width: 34px;
    height: 34px;
    place-items: center;
    border-radius: 50%;
    background: #fff0f3;
    color: #8c1a3e;
  }

  .santa-rita-tour-detail__metric span svg {
    width: 18px;
    height: 18px;
  }

  .santa-rita-tour-detail__metric strong {
    color: #21000f;
    font-size: 18px;
    font-weight: 700;
    line-height: 1.15;
  }

  .santa-rita-tour-detail__metric small {
    color: #6e5a61;
    font-size: 14px;
    line-height: 1.15;
  }

  .santa-rita-tour-detail__layout {
    display: grid;
    grid-template-columns: minmax(0, 100%) minmax(260px, 330px);
    justify-content: center;
    gap: 32px;
    align-items: start;
  }

  .santa-rita-tour-detail__main {
    display: grid;
    gap: 34px;
  }

  .santa-rita-tour-detail__route {
    min-height: 445px;
    border-radius: 8px;
    background: #250012;
    color: #ffffff;
    padding: 48px 64px;
  }

  .santa-rita-tour-detail h2 {
    margin: 0 0 22px;
    font-family: var(--font-heading--family);
    font-size: clamp(22px, 2vw, 28px);
    line-height: 1.1;
    letter-spacing: 0;
  }

  .santa-rita-tour-detail__route-items {
    display: grid;
    gap: 0;
  }

  .santa-rita-tour-detail__route-item {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    column-gap: 18px;
    align-items: center;
    border-top: 1px solid rgba(255, 255, 255, 0.14);
    padding-block: 20px;
  }

  .santa-rita-tour-detail__route-item:first-child {
    border-top: 0;
  }

  .santa-rita-tour-detail__route-item span {
    display: grid;
    width: 38px;
    height: 38px;
    place-items: center;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.14);
    font-size: 20px;
    font-weight: 700;
    line-height: 1;
  }

  .santa-rita-tour-detail__route-item h3 {
    margin: 0;
    font-family: var(--font-heading--family);
    font-size: 17px;
    font-weight: 700;
    line-height: 1.25;
    letter-spacing: 0;
  }

  .santa-rita-tour-detail__route-item div {
    grid-column: 2;
    margin-top: 6px;
    color: rgba(255, 255, 255, 0.78);
    font-size: 13px;
    line-height: 1.4;
  }

  .santa-rita-tour-detail__route-item div p,
  .santa-rita-tour-detail__experience div p {
    margin: 0;
  }

  .santa-rita-tour-detail__route-item div p + p,
  .santa-rita-tour-detail__experience div p + p {
    margin-top: 12px;
  }

  .santa-rita-tour-detail__experience {
    width: min(100%, 690px);
    padding-inline: 34px;
  }

  .santa-rita-tour-detail__experience h2 {
    margin-bottom: 18px;
  }

  .santa-rita-tour-detail__experience div {
    color: #2f2026;
    font-size: 14px;
    line-height: 1.62;
  }

  .santa-rita-tour-detail__includes {
    width: min(100%, 690px);
    border-radius: 8px;
    background: #fff8f1;
    padding: 32px 36px;
  }

  .santa-rita-tour-detail__includes-grid {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 18px;
  }

  .santa-rita-tour-detail__include {
    display: grid;
    justify-items: center;
    gap: 10px;
    text-align: center;
  }

  .santa-rita-tour-detail__include span {
    display: grid;
    width: 42px;
    height: 42px;
    place-items: center;
    border-radius: 50%;
    background: #fff0e7;
    color: #8c1a3e;
    font-size: 18px;
  }

  .santa-rita-tour-detail__include p {
    margin: 0;
    color: #4f3c43;
    font-size: 12px;
    line-height: 1.25;
  }

  .santa-rita-tour-detail__reserve {
    position: sticky;
    top: calc(var(--header-height, 0px) + 20px);
    border-radius: 8px;
    overflow: hidden;
    border: 1px solid #8c1a3e;
    background: #ffffff;
    color: #21000f;
    box-shadow: 0 10px 26px rgba(140, 26, 62, 0.16);
  }

  .santa-rita-tour-detail__reserve h2 {
    display: flex;
    min-height: 48px;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin: 0;
    background: #8c1a3e;
    color: #ffffff;
    font-size: 17px;
    padding: 14px 18px;
  }

  .santa-rita-tour-detail__reserve h2 span {
    display: grid;
    width: 38px;
    height: 38px;
    flex: 0 0 38px;
    place-items: center;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.16);
  }

  .santa-rita-tour-detail__reserve h2 svg {
    width: 18px;
    height: 18px;
  }

  .santa-rita-tour-detail__reserve-body {
    padding: 20px 18px 18px;
  }

  .santa-rita-tour-detail__reserve-info-heading {
    margin: 0 0 18px;
    font-family: var(--font-heading--family);
    color: #21000f;
    font-size: 15px;
    line-height: 1.2;
  }

  .santa-rita-tour-detail__form {
    display: grid;
    gap: 10px;
  }

  .santa-rita-tour-detail__fields {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }

  .santa-rita-tour-detail__field {
    position: relative;
    display: grid;
  }

  .santa-rita-tour-detail__field label {
    position: absolute;
    top: 6px;
    left: 10px;
    z-index: 1;
    color: #21000f;
    font-size: 9px;
    font-weight: 400;
    line-height: 1.2;
    opacity: 0.58;
    pointer-events: none;
  }

  .santa-rita-tour-detail__field input,
  .santa-rita-tour-detail__field select {
    width: 100%;
    min-height: 41px;
    border: 1px solid rgba(33, 0, 15, 0.18);
    border-radius: 4px;
    background: #ffffff;
    color: #21000f;
    font: inherit;
    font-size: 13px;
    line-height: 1.1;
    padding: 16px 28px 5px 10px;
  }

  .santa-rita-tour-detail__field--calendar > label,
  .santa-rita-tour-detail__field--calendar > input {
    display: none;
  }

  .santa-rita-tour-detail__reserve .flatpickr-calendar {
    width: 100%;
    margin-top: 0;
    border: 1px solid rgba(33, 0, 15, 0.16);
    border-radius: 6px;
    font-size: 11px;
    box-shadow: none;
  }

  .santa-rita-tour-detail__reserve .flatpickr-months {
    align-items: center;
  }

  .santa-rita-tour-detail__reserve .flatpickr-month,
  .santa-rita-tour-detail__reserve .flatpickr-current-month {
    height: 30px;
  }

  .santa-rita-tour-detail__reserve .flatpickr-current-month {
    padding-top: 2px;
    font-size: 12px;
  }

  .santa-rita-tour-detail__reserve .flatpickr-current-month .numInputWrapper {
    display: none;
  }

  .santa-rita-tour-detail__reserve .flatpickr-weekday {
    color: #8c1a3e;
    font-size: 10px;
    font-weight: 700;
  }

  .santa-rita-tour-detail__reserve .flatpickr-rContainer,
  .santa-rita-tour-detail__reserve .flatpickr-days,
  .santa-rita-tour-detail__reserve .dayContainer {
    width: 100%;
    min-width: 100%;
    max-width: 100%;
  }

  .santa-rita-tour-detail__reserve .flatpickr-day {
    height: 27px;
    line-height: 27px;
    max-width: none;
    color: #21000f;
    font-size: 11px;
  }

  .santa-rita-tour-detail__reserve .flatpickr-day.selected,
  .santa-rita-tour-detail__reserve .flatpickr-day.startRange,
  .santa-rita-tour-detail__reserve .flatpickr-day.endRange,
  .santa-rita-tour-detail__reserve .flatpickr-day.today {
    border-color: #8c1a3e;
    background: #8c1a3e;
    color: #ffffff;
  }

  .santa-rita-tour-detail__reserve .flatpickr-day.selected:hover,
  .santa-rita-tour-detail__reserve .flatpickr-day.startRange:hover,
  .santa-rita-tour-detail__reserve .flatpickr-day.endRange:hover,
  .santa-rita-tour-detail__reserve .flatpickr-day.today:hover {
    border-color: #8c1a3e;
    background: #8c1a3e;
    color: #ffffff;
  }

  .santa-rita-tour-detail__total {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    border-top: 0;
    padding-top: 8px;
    color: #21000f;
    font-size: 16px;
  }

  .santa-rita-tour-detail__total strong {
    font-size: 16px;
    font-weight: 700;
  }

  .santa-rita-tour-detail__submit {
    display: inline-flex;
    width: 100%;
    min-height: 39px;
    cursor: pointer;
    align-items: center;
    justify-content: center;
    border: 0;
    border-radius: 4px;
    background: #8c1a3e;
    color: #ffffff;
    font: inherit;
    font-size: 13px;
    font-weight: 700;
  }

  .santa-rita-tour-detail__submit:disabled {
    cursor: not-allowed;
    opacity: 0.62;
  }

  @media screen and (max-width: 989px) {
    .santa-rita-tour-detail__layout {
      grid-template-columns: 1fr;
    }

    .santa-rita-tour-detail__reserve {
      position: static;
      max-width: 460px;
    }
  }

  @media screen and (max-width: 749px) {
    .santa-rita-tour-detail__hero-inner,
    .santa-rita-tour-detail__content {
      width: calc(100% - 24px);
    }

    .santa-rita-tour-detail__hero {
      min-height: 380px;
    }

    .santa-rita-tour-detail__breadcrumbs {
      display: none;
    }

    .santa-rita-tour-detail__hero-inner {
      padding-block: 22px 34px;
    }

    .santa-rita-tour-detail__hero-content {
      text-align: left;
    }

    .santa-rita-tour-detail__hero-content p {
      margin-inline: 0;
    }

    .santa-rita-tour-detail__content {
      padding-block: 24px 50px;
    }

    .santa-rita-tour-detail__metrics {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px;
    }

    .santa-rita-tour-detail__route,
    .santa-rita-tour-detail__experience,
    .santa-rita-tour-detail__includes {
      padding: 24px;
    }

    .santa-rita-tour-detail__route {
      min-height: 0;
    }

    .santa-rita-tour-detail__reserve {
      padding: 0;
    }

    .santa-rita-tour-detail__includes-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .santa-rita-tour-detail__fields {
      grid-template-columns: 1fr;
    }
  }
</style>

{% schema %}
{
  "name": "Detalle tour metaobjeto",
  "settings": [
    {
      "type": "page",
      "id": "tours_page",
      "label": "Pagina listado de tours"
    },
    {
      "type": "text",
      "id": "price_note",
      "label": "Nota precio",
      "default": "CLP / por persona"
    },
    {
      "type": "header",
      "content": "Reserva"
    },
    {
      "type": "url",
      "id": "booking_url",
      "label": "URL reserva por defecto",
      "info": "Se usa solo si el metaobjeto no tiene booking_url."
    },
    {
      "type": "text",
      "id": "reserve_heading",
      "label": "Titulo reserva",
      "default": "Haz tu reserva"
    },
    {
      "type": "text",
      "id": "reserve_info_heading",
      "label": "Titulo informacion reserva",
      "default": "Informacion de tu reserva"
    },
    {
      "type": "text",
      "id": "date_label",
      "label": "Etiqueta fecha",
      "default": "Fecha"
    },
    {
      "type": "text",
      "id": "date_placeholder",
      "label": "Placeholder fecha",
      "default": "Selecciona una fecha"
    },
    {
      "type": "text",
      "id": "adults_label",
      "label": "Etiqueta adultos",
      "default": "Adultos"
    },
    {
      "type": "text",
      "id": "children_label",
      "label": "Etiqueta ninos",
      "default": "Ninos"
    },
    {
      "type": "range",
      "id": "max_people",
      "label": "Maximo personas por defecto",
      "min": 1,
      "max": 30,
      "step": 1,
      "default": 12
    },
    {
      "type": "text",
      "id": "time_label",
      "label": "Etiqueta horario",
      "default": "Horario"
    },
    {
      "type": "text",
      "id": "time_options",
      "label": "Horarios por defecto",
      "default": "09:45, 11:30, 13:00, 15:30",
      "info": "Separados por coma."
    },
    {
      "type": "text",
      "id": "language_label",
      "label": "Etiqueta idioma",
      "default": "Idioma"
    },
    {
      "type": "text",
      "id": "language_options",
      "label": "Idiomas por defecto",
      "default": "Espanol, Ingles, Portugues",
      "info": "Separados por coma."
    },
    {
      "type": "text",
      "id": "total_label",
      "label": "Etiqueta total",
      "default": "Total"
    },
    {
      "type": "text",
      "id": "reserve_button_label",
      "label": "Texto boton reserva",
      "default": "Reservar Tour"
    }
  ],
  "presets": [
    {
      "name": "Detalle tour metaobjeto"
    }
  ]
}
{% endschema %}
