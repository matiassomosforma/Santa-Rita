{% liquid
  assign banner_image = section.settings.desktop_image | default: section.settings.mobile_image
  assign mobile_image = section.settings.mobile_image | default: banner_image
  assign banner_title = section.settings.heading | default: page.title
  assign banner_subtitle = section.settings.subheading
  assign breadcrumb_label = page.title | default: banner_title
  assign banner_style = section.settings.banner_style | default: 'background'
  assign text_alignment = section.settings.text_alignment | default: 'left'
  assign mobile_text_alignment = section.settings.mobile_text_alignment | default: text_alignment

  case text_alignment
    when 'center'
      assign content_margin_inline = 'auto'
      assign text_margin_inline = 'auto'
    when 'right'
      assign content_margin_inline = 'auto 0'
      assign text_margin_inline = 'auto 0'
    else
      assign content_margin_inline = '0 auto'
      assign text_margin_inline = '0 auto'
  endcase

  case mobile_text_alignment
    when 'center'
      assign mobile_content_margin_inline = 'auto'
      assign mobile_text_margin_inline = 'auto'
    when 'right'
      assign mobile_content_margin_inline = 'auto 0'
      assign mobile_text_margin_inline = 'auto 0'
    else
      assign mobile_content_margin_inline = '0 auto'
      assign mobile_text_margin_inline = '0 auto'
  endcase
%}

<section
  class="santa-rita-page-banner santa-rita-page-banner--{{ banner_style }}"
  style="
    --banner-padding-top: {{ section.settings.desktop_padding_top | default: 28 }}px;
    --banner-padding-bottom: {{ section.settings.desktop_padding_bottom | default: 74 }}px;
    --banner-mobile-padding-top: {{ section.settings.mobile_padding_top | default: 14 }}px;
    --banner-mobile-padding-bottom: {{ section.settings.mobile_padding_bottom | default: 18 }}px;
    --banner-text-align: {{ text_alignment }};
    --banner-content-margin-inline: {{ content_margin_inline }};
    --banner-text-margin-inline: {{ text_margin_inline }};
    --banner-mobile-text-align: {{ mobile_text_alignment }};
    --banner-mobile-content-margin-inline: {{ mobile_content_margin_inline }};
    --banner-mobile-text-margin-inline: {{ mobile_text_margin_inline }};
  "
  aria-labelledby="santa-rita-page-banner-title-{{ section.id }}"
  {{ section.shopify_attributes }}
>
  {% capture banner_media %}
    {% if banner_image != blank %}
      <picture class="santa-rita-page-banner__picture">
        {% if mobile_image != blank %}
          <source
            media="(max-width: 749px)"
            srcset="
              {{ mobile_image | image_url: width: 375 }} 375w,
              {{ mobile_image | image_url: width: 550 }} 550w,
              {{ mobile_image | image_url: width: 750 }} 750w
            "
            sizes="100vw"
          >
        {% endif %}
        {{
          banner_image
          | image_url: width: 2200
          | image_tag:
            widths: '375, 550, 750, 990, 1440, 1800, 2200',
            sizes: '100vw',
            loading: 'eager',
            class: 'santa-rita-page-banner__image'
        }}
      </picture>
    {% elsif request.design_mode %}
      {{ 'image' | placeholder_svg_tag: 'santa-rita-page-banner__image santa-rita-page-banner__image--placeholder' }}
    {% endif %}
  {% endcapture %}
  {% assign banner_media = banner_media | strip %}

  {% if banner_style == 'background' %}
    {{ banner_media }}
  {% endif %}

  <div class="santa-rita-page-banner__overlay"></div>

  <div class="santa-rita-page-banner__inner">
    {% if section.settings.show_breadcrumbs %}
      <nav class="santa-rita-page-banner__breadcrumbs" aria-label="Breadcrumb">
        <a href="{{ routes.root_url }}">Inicio</a>
        <span aria-hidden="true">&gt;</span>
        <span>{{ breadcrumb_label }}</span>
      </nav>
    {% endif %}

    <div class="santa-rita-page-banner__grid">
      <div class="santa-rita-page-banner__content">
        {% if banner_title != blank %}
          <h1 id="santa-rita-page-banner-title-{{ section.id }}">{{ banner_title }}</h1>
        {% endif %}

        {% if banner_subtitle != blank %}
          <p>{{ banner_subtitle }}</p>
        {% endif %}
      </div>

      {% if banner_style == 'split' and banner_media != blank %}
        <div class="santa-rita-page-banner__media">
          {{ banner_media }}
        </div>
      {% endif %}
    </div>
  </div>
</section>

<style>
  .santa-rita-page-banner {
    position: relative;
    display: grid;
    min-height: clamp(320px, 31vw, 430px);
    overflow: hidden;
    place-items: stretch;
    background: #09090b;
    color: #ffffff;
  }

  .santa-rita-page-banner__picture,
  .santa-rita-page-banner__image,
  .santa-rita-page-banner__image--placeholder {
    position: absolute;
    inset: 0;
    display: block;
    width: 100%;
    height: 100%;
  }

  .santa-rita-page-banner__image {
    object-fit: cover;
    object-position: center;
  }

  .santa-rita-page-banner__image--placeholder {
    background: #1f1118;
    fill: rgba(255, 255, 255, 0.2);
  }

  .santa-rita-page-banner__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(90deg, rgba(0, 0, 0, 0.76) 0%, rgba(0, 0, 0, 0.48) 36%, rgba(0, 0, 0, 0.08) 75%);
  }

  .santa-rita-page-banner__inner {
    position: relative;
    z-index: 1;
    display: flex;
    width: min(100% - 48px, 1220px);
    min-height: inherit;
    flex-direction: column;
    margin-inline: auto;
    padding-block: var(--banner-padding-top) var(--banner-padding-bottom);
  }

  .santa-rita-page-banner__breadcrumbs {
    display: flex;
    align-items: center;
    gap: 7px;
    color: #ffffff;
    font-size: 12px;
    line-height: 1.2;
  }

  .santa-rita-page-banner__breadcrumbs a {
    color: inherit;
    text-decoration: none;
  }

  .santa-rita-page-banner__content {
    width: min(100%, 750px);
    margin-block: auto 0;
    margin-inline: var(--banner-content-margin-inline);
    text-align: var(--banner-text-align);
  }

  .santa-rita-page-banner__grid {
    display: block;
    margin-block: auto 0;
  }

  .santa-rita-page-banner__content h1 {
    margin: 0 0 18px;
    font-family: var(--font-heading--family);
    font-size: clamp(38px, 4vw, 56px);
    font-weight: 700;
    line-height: normal;
    letter-spacing: 0;
  }

  .santa-rita-page-banner__content p {
    max-width: 520px;
    margin: 0;
    margin-inline: var(--banner-text-margin-inline);
    font-size: 24px;
    line-height: normal;
  }

  .santa-rita-page-banner--split {
    min-height: 350px;
    overflow: visible;
    border-bottom: 1px solid rgba(110, 90, 97, 0.18);
    background: #fffbf6;
    color: #21000f;
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__overlay {
    display: none;
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__inner {
    display: grid;
    min-height: 350px;
    padding-block: var(--banner-padding-top) var(--banner-padding-bottom);
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__breadcrumbs {
    color: #21000f;
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(420px, 620px);
    gap: 72px;
    align-items: center;
    margin-block: 0;
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__content {
    width: 100%;
    margin: 0;
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__content h1 {
    margin-bottom: 22px;
    font-size: clamp(38px, 4.1vw, 58px);
    line-height: 1.05;
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__content p {
    max-width: 520px;
    font-size: 24px;
    line-height: 1.15;
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__media {
    width: 100%;
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__picture,
  .santa-rita-page-banner--split .santa-rita-page-banner__image,
  .santa-rita-page-banner--split .santa-rita-page-banner__image--placeholder {
    position: static;
    inset: auto;
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__picture {
    aspect-ratio: 2.28 / 1;
    overflow: hidden;
    border-radius: 8px;
  }

  .santa-rita-page-banner--split .santa-rita-page-banner__image,
  .santa-rita-page-banner--split .santa-rita-page-banner__image--placeholder {
    height: 100%;
    object-fit: cover;
  }

  @media screen and (max-width: 749px) {
    .santa-rita-page-banner {
      min-height: clamp(172px, 55vw, 230px);
      place-items: end start;
    }

    .santa-rita-page-banner__image {
      object-position: center;
    }

    .santa-rita-page-banner__overlay {
      background: linear-gradient(180deg, rgba(0, 0, 0, 0.08) 0%, rgba(0, 0, 0, 0.28) 44%, rgba(0, 0, 0, 0.72) 100%);
    }

    .santa-rita-page-banner__inner {
      width: calc(100% - 24px);
      min-height: inherit;
      padding-block: var(--banner-mobile-padding-top) var(--banner-mobile-padding-bottom);
    }

    .santa-rita-page-banner__breadcrumbs {
      display: none;
    }

    .santa-rita-page-banner__content {
      width: min(100%, 270px);
      margin-top: auto;
      margin-inline: var(--banner-mobile-content-margin-inline);
      text-align: var(--banner-mobile-text-align);
    }

    .santa-rita-page-banner__content h1 {
      margin-bottom: 7px;
      font-size: clamp(18px, 6.2vw, 24px);
      line-height: normal;
    }

    .santa-rita-page-banner__content p {
      max-width: min(100%, 230px);
      margin-inline: var(--banner-mobile-text-margin-inline);
      font-size: clamp(9px, 3.1vw, 12px);
      line-height: 1.25;
    }

    .santa-rita-page-banner--split {
      min-height: 0;
      border-bottom: 0;
      background: #fffbf6;
      color: #21000f;
    }

    .santa-rita-page-banner--split .santa-rita-page-banner__inner {
      display: block;
      width: calc(100% - 28px);
      min-height: 0;
      padding-block: var(--banner-mobile-padding-top) var(--banner-mobile-padding-bottom);
    }

    .santa-rita-page-banner--split .santa-rita-page-banner__breadcrumbs {
      display: flex;
      margin-bottom: 30px;
      color: #21000f;
      font-size: 12px;
    }

    .santa-rita-page-banner--split .santa-rita-page-banner__grid {
      display: block;
    }

    .santa-rita-page-banner--split .santa-rita-page-banner__content {
      width: 100%;
    }

    .santa-rita-page-banner--split .santa-rita-page-banner__content h1 {
      margin-bottom: 18px;
      font-size: clamp(38px, 12vw, 48px);
      line-height: 1;
    }

    .santa-rita-page-banner--split .santa-rita-page-banner__content p {
      max-width: 330px;
      font-size: 21px;
      line-height: 1.18;
    }

    .santa-rita-page-banner--split .santa-rita-page-banner__media {
      display: none;
    }
  }
</style>

{% schema %}
{
  "name": "Banner de pagina",
  "settings": [
    {
      "type": "select",
      "id": "banner_style",
      "label": "Tipo de banner",
      "options": [
        {
          "value": "background",
          "label": "Imagen de fondo"
        },
        {
          "value": "split",
          "label": "Texto e imagen"
        }
      ],
      "default": "background"
    },
    {
      "type": "image_picker",
      "id": "desktop_image",
      "label": "Imagen desktop"
    },
    {
      "type": "image_picker",
      "id": "mobile_image",
      "label": "Imagen mobile"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Titulo",
      "info": "Si queda vacio se usara el titulo de la pagina."
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subtitulo"
    },
    {
      "type": "checkbox",
      "id": "show_breadcrumbs",
      "label": "Mostrar breadcrumb en desktop",
      "default": true
    },
    {
      "type": "header",
      "content": "Espaciado desktop"
    },
    {
      "type": "range",
      "id": "desktop_padding_top",
      "label": "Padding superior desktop",
      "min": 0,
      "max": 200,
      "step": 2,
      "unit": "px",
      "default": 28
    },
    {
      "type": "range",
      "id": "desktop_padding_bottom",
      "label": "Padding inferior desktop",
      "min": 0,
      "max": 200,
      "step": 2,
      "unit": "px",
      "default": 74
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Alineacion texto desktop",
      "options": [
        {
          "value": "left",
          "label": "Izquierda"
        },
        {
          "value": "center",
          "label": "Centro"
        },
        {
          "value": "right",
          "label": "Derecha"
        }
      ],
      "default": "left"
    },
    {
      "type": "header",
      "content": "Espaciado mobile"
    },
    {
      "type": "range",
      "id": "mobile_padding_top",
      "label": "Padding superior mobile",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 14
    },
    {
      "type": "range",
      "id": "mobile_padding_bottom",
      "label": "Padding inferior mobile",
      "min": 0,
      "max": 160,
      "step": 2,
      "unit": "px",
      "default": 18
    },
    {
      "type": "select",
      "id": "mobile_text_alignment",
      "label": "Alineacion texto mobile",
      "options": [
        {
          "value": "left",
          "label": "Izquierda"
        },
        {
          "value": "center",
          "label": "Centro"
        },
        {
          "value": "right",
          "label": "Derecha"
        }
      ],
      "default": "left"
    }
  ],
  "presets": [
    {
      "name": "Banner de pagina"
    }
  ]
}
{% endschema %}
